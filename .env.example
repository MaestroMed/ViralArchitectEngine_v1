# FORGE/LAB environment configuration. Copy to .env and adjust.
# Every engine variable is read by forge_engine.core.config.Settings via the
# FORGE_ prefix (pydantic-settings).

# --- Debug / observability ---
# Enable FastAPI /docs + /redoc and widen CORS to common local dev ports.
# Leave unset (or =0) in production.
# FORGE_DEBUG=0

# --- Paths ---
# Where projects, the SQLite DB, and the temp workspace live.
# FORGE_LIBRARY_PATH=/home/you/FORGE_LIBRARY

# Additional import roots the API will accept as source_path (OS path-sep or
# comma separated). LIBRARY_PATH and $HOME are always allowed.
# FORGE_ALLOWED_IMPORT_ROOTS=/mnt/recordings:/mnt/obs

# --- Networking ---
# FORGE_HOST=127.0.0.1
# FORGE_PORT=8420
# CORS allowlist used when FORGE_DEBUG is off (JSON array).
# FORGE_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# --- LAN access (for the iPhone app) ---
# When 1: bind to 0.0.0.0 so phones on the home WiFi can reach the engine,
# and FORCE auth on (any /v1 request must carry a valid X-API-Key header).
# Create the first key with:
#   python -m forge_engine.scripts.seed_api_key create "iPhone Air"
# FORGE_BIND_LAN=0

# Force auth without widening the bind (useful for testing on localhost).
# FORGE_REQUIRE_AUTH=0

# Tweak this if your home network isn't on 10/8, 172.16/12, or 192.168/16.
# FORGE_LAN_CORS_REGEX=...

# --- Job queue ---
# Sequential by default for GPU/FFmpeg stability. Raise when the host has
# headroom (multi-GPU, CPU-only pipelines).
# FORGE_MAX_WORKERS=1

# --- Morning automation (Phase 3) ---
# IANA timezone for the publish scheduler + export window (default Europe/Paris).
# FORGE_TIMEZONE=Europe/Paris
# Defer the GPU-heavy auto-export to this window so clips are fresh at wake-up
# instead of rendered late at night. Unset = export immediately after analysis.
# FORGE_EXPORT_WINDOW=05:00-07:00
# Shared secret for Twitch EventSub. When set, POST /v1/webhooks/twitch reacts
# to stream.offline instantly (HMAC-verified) instead of waiting for the poll.
# FORGE_TWITCH_WEBHOOK_SECRET=

# --- GPU / ML ---
# FORGE_FORCE_CPU=0
# FORGE_WHISPER_MODEL=large-v3
# FORGE_WHISPER_DEVICE=cuda       # cuda | cpu
# FORGE_WHISPER_COMPUTE_TYPE=float16  # float16 | int8 | float32

# --- FFmpeg ---
# FORGE_FFMPEG_PATH=ffmpeg
# FORGE_FFPROBE_PATH=ffprobe
