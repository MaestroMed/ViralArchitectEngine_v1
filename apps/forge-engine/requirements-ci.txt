# CI-only dependencies — lightweight, no GPU/ML required
# Used by GitHub Actions to run unit tests

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Data Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
aiosqlite>=0.19.0
sqlalchemy[asyncio]>=2.0.23

# HTTP client
httpx>=0.25.0
aiofiles>=23.2.0

# Encryption at rest for social-publish OAuth tokens (core/crypto.py).
# Imported at app startup via social_publish → credential_store.
cryptography>=42.0.0

# System metrics — monitor.py imports psutil at module load, so any test that
# imports the app or MonitorService (e.g. test_services.py) needs it.
psutil>=5.9.0

# Minimal ML (pure Python, no CUDA needed)
numpy>=1.26.0
scikit-learn>=1.3.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
